import { BindPopupDirective } from './bind-popup.directive';

describe('BindPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new BindPopupDirective();
    expect(directive).toBeTruthy();
  });
});
