import { DynamicViewComponent } from './dynamic-view.component';

describe('DynamicView', () => {
  it('should create an instance', () => {
    expect(new DynamicViewComponent()).toBeTruthy();
  });
});
